# Summary

<!-- What does this PR change and why? Link issues if any. -->

## Checklist

- [ ] Tests added/updated and passing locally (`python3 test_contrast.py && python3 test_audit.py && python3 test_dom.py && python3 test_mcp.py`)
- [ ] Zero dependencies added to the core (optional enhancements behind import guards only)
- [ ] New user-facing strings exist in **es and en** (follow the `T`/`CRIT` pattern)
- [ ] Scope notes kept honest — no implication of WCAG conformance from automated passes
- [ ] `CHANGELOG.md` entry if user-facing; `pyproject.toml` + `server.VERSION` bumped together
