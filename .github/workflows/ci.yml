name: CI
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.10', '3.12']
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: pip install pillow
      - run: python tests/test_contrast.py
      - run: python tests/test_audit.py
      - run: python tests/test_dom.py     # self-skips if Playwright is absent
      - run: python tests/test_v32.py
      - run: python tests/test_solido.py
      - run: python tests/test_cli.py
      - run: python tests/test_mcp.py
      - name: Package smoke test (pip install .)
        run: |
          pip install .
          a11ytoolkit pair "#767676" "#ffffff"
          python3 -c "from a11y_toolkit import server, a11ydom; print('server imports OK,', len(server.TOOLS), 'tools,', len(server._PROMPTS), 'prompts')"
          pip uninstall -y a11y-toolkit
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install build
      - run: python -m build
