# Smithery configuration: https://smithery.ai/docs/config
startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction: |-
    (config) => ({
      command: 'uvx',
      args: ['--from', 'a11y-toolkit', 'a11y-toolkit-mcp']
    })
